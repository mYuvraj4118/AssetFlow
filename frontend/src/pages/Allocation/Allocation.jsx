import React from 'react';
import PageContainer from '../../components/layout/PageContainer';

const Allocation = () => {
  return (
    <PageContainer title="Allocation">
      <div className="nm-flat p-lg rounded-lg text-main">
        <p>Allocate assets to users, departments, or locations. This page is currently under construction.</p>
      </div>
    </PageContainer>
  );
};

export default Allocation;
